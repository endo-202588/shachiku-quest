.container.max-w-3xl.mx-auto.px-4.py-8
  h1.text-2xl.font-bold.text-gray-800.mb-6 完了通知の確認

  - if @unread_completed_requests.any?
    .mb-4.p-3.rounded.bg-yellow-50.text-yellow-800.border.border-yellow-200
      | 🔔 未読の完了通知が #{@unread_completed_requests.count} 件あります

  - if @completed_requests.any?
    ul.divide-y.divide-gray-200.bg-white.rounded-lg.shadow-md
      - @completed_requests.each do |hr|
        li.p-4.flex.items-start.justify-between.gap-4
          .min-w-0
            .flex.items-center.gap-2
              = link_to hr.task.title, help_request_path(hr), class: "font-medium text-gray-900 hover:underline"
              - if hr.completed_read_at.nil?
                span.px-2.py-1.text-xs.rounded-full.bg-yellow-400.text-yellow-900.font-bold 未読

            .mt-1.text-sm.text-gray-600
              | 完了通知: #{l(hr.completed_notified_at, format: :short) rescue hr.completed_notified_at}

              - if hr.helper.present?
                |  ／ ヘルパー: #{hr.helper.decorate.full_name rescue hr.helper.name}

          = link_to "開く", help_request_path(hr),
            class: "shrink-0 inline-flex items-center px-3 py-1 rounded-lg border border-gray-300 text-gray-700 hover:bg-gray-50 text-sm"
  - else
    p.text-gray-600.text-sm 完了通知はまだありません。
