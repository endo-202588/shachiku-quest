.container.max-w-xl.mx-auto.px-4.py-8
  .bg-white.rounded-lg.shadow-md.p-6
    h1.text-xl.font-bold.text-gray-800.mb-4 🔮 魔法を編集

    = form_with model: @help_magic, url: help_magic_path, method: :patch, local: true, class: "space-y-4" do |f|
    
      = hidden_field_tag :return_to, params[:return_to]

      - if @help_magic.errors.any?
        .bg-red-50.border.border-red-200.rounded-lg.p-4
          ul.list-disc.pl-5.text-sm.text-red-700
            - @help_magic.errors.full_messages.each do |msg|
              li = msg

      .space-y-1
        = f.label :available_time, "時間", class: "block text-sm font-medium text-gray-700"
        = f.select :available_time,
          HelpMagic.available_times_i18n.invert,
          {},
          class: "block w-full rounded-md border-gray-300 shadow-sm"

      .flex.justify-end.gap-2.pt-2
        = link_to "キャンセル", safe_return_path(dashboard_path), class: "px-4 py-2 rounded-lg border border-gray-300 text-gray-700 hover:bg-gray-50"
        = f.submit "更新する", class: "px-4 py-2 rounded-lg bg-purple-600 text-white hover:bg-purple-700"
