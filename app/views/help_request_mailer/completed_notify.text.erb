<%= @owner.decorate.full_name rescue @owner.name %> さん

ヘルパー（<%= @helper&.decorate&.full_name || @helper&.name || "不明" %>）が、
以下のヘルプ要請タスクについて「完了通知」を送りました。

完了通知日時:
<%= (@help_request.completed_notified_at&.strftime("%Y年%m月%d日 %H:%M") || "未記録") %>（日本時間）

タスク: <%= @task.title %>

詳細はこちら:
<%= task_url(@task) %>

---
シャチーク
